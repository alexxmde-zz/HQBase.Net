@model IEnumerable<HQBase.Models.Comic>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Collection.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Drawer.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Publisher.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Writer.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EditionNum)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Collection.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Drawer.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Publisher.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Writer.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EditionNum)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ComicId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ComicId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ComicId })
        </td>
    </tr>
}

</table>
